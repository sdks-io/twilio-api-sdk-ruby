
# Verification Enum Status

The status of the verification. Can be: `pending`, `approved`, `canceled`, `max_attempts_reached`, `deleted`, `failed` or `expired`.

## Enumeration

`VerificationEnumStatus`

## Fields

| Name |
|  --- |
| `CANCELED` |
| `APPROVED` |

