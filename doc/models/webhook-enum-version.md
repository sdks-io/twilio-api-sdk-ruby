
# Webhook Enum Version

The webhook version. Default value is `v2` which includes all the latest fields. Version `v1` is legacy and may be removed in the future.

## Enumeration

`WebhookEnumVersion`

## Fields

| Name |
|  --- |
| `V1` |
| `V2` |

