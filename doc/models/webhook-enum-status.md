
# Webhook Enum Status

The webhook status. Default value is `enabled`. One of: `enabled` or `disabled`

## Enumeration

`WebhookEnumStatus`

## Fields

| Name |
|  --- |
| `ENABLED` |
| `DISABLED` |

