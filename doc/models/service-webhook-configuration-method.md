
# Service Webhook Configuration Method

The HTTP method to be used when sending a webhook request. One of `GET` or `POST`.

## Enumeration

`ServiceWebhookConfigurationMethod`

## Fields

| Name |
|  --- |
| `GET` |
| `POST` |

